<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 定义dialog的样式-->
    <style name="CustomDialog" parent="Theme.AppCompat.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/dialogAnimSlideBottom</item>
    </style>
    <style name="dialogAnimFade" parent="android:Animation">
        <item name="android:interpolator">@android:anim/decelerate_interpolator</item>
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>
    <style name="dialogAnimSlideBottom" parent="android:Animation">
        <item name="android:interpolator">@android:anim/decelerate_interpolator</item>
        <item name="android:windowEnterAnimation">@anim/dialog_slide_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/dialog_slide_bottom_out</item>
    </style>

    <style name="dialogAnimScale" parent="android:Animation">
        <item name="android:interpolator">@android:anim/decelerate_interpolator</item>
        <item name="android:windowEnterAnimation">@anim/dialog_scale_in</item>
        <item name="android:windowExitAnimation">@anim/dialogt_scale_out</item>
    </style>
</resources>